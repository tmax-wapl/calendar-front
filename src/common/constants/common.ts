export enum MODE {
  FULL = 'full',
  MODULE = 'module',
}

export enum DATE_EVENT {
  PREV = 'prev',
  NEXT = 'next',
  TODAY = 'today',
}

export enum VIEW_MODE {
  MONTH = 'dayGridMonth',
  WEEK = 'timeGridWeek',
  DAY = 'timeGridDay',
}
