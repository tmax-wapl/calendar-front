import { styled, Mui } from '@wapl/ui';
const { IconButton } = Mui;

export const DatePickerContainer = styled.div``;

export const DatePickerHeader = styled.div`
  width: 100%;
  height: 30px;
`;

export const SwitchViewButton = styled(IconButton)``;
